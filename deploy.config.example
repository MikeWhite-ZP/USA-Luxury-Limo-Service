# ==================================
# USA Luxury Limo - Deployment Configuration
# ==================================
# Copy this file to 'deploy.config' and fill in your values
# cp deploy.config.example deploy.config
# ==================================

# ----------------------------------
# Application Settings
# ----------------------------------
# Port for the application (default: 5000)
PORT=5000

# Application directory (leave empty to use current directory)
APP_DIR="/opt/usa-luxury-limo"

# GitHub repository URL (optional - leave empty to use local files)
# GITHUB_REPO="https://github.com/yourusername/USA-Luxury-Limo-Service.git"
GITHUB_REPO=""

# Domain name for your application (optional)
DOMAIN="your-domain.com"

# ----------------------------------
# Database Configuration (REQUIRED)
# ----------------------------------
# PostgreSQL connection string from Neon or your database provider
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ----------------------------------
# Security (REQUIRED)
# ----------------------------------
# Session secret - Generate with: openssl rand -base64 32
SESSION_SECRET="generate-with-openssl-rand-base64-32"

# ----------------------------------
# Stripe Payment Processing (REQUIRED)
# ----------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_live_your_stripe_secret_key"
VITE_STRIPE_PUBLIC_KEY="pk_live_your_stripe_public_key"

# ----------------------------------
# MinIO Object Storage (REQUIRED)
# ----------------------------------
# MinIO settings for file uploads
MINIO_ENDPOINT="http://minio:9000"
MINIO_ACCESS_KEY="minioadmin"
# Generate a strong password for MinIO
MINIO_SECRET_KEY="your-strong-minio-password-change-this"
MINIO_BUCKET="usa-luxury-limo"

# ----------------------------------
# Email Configuration (OPTIONAL)
# ----------------------------------
# SMTP settings for sending emails
# For Gmail: smtp.gmail.com:587
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"

EMAIL_FROM_NAME="USA Luxury Limo"
EMAIL_FROM_ADDRESS="noreply@your-domain.com"

# ----------------------------------
# SMS Configuration (OPTIONAL)
# ----------------------------------
# Twilio settings for SMS notifications
# Get these from: https://console.twilio.com
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your_twilio_auth_token"
TWILIO_PHONE_NUMBER="+1234567890"

# ----------------------------------
# API Keys (OPTIONAL)
# ----------------------------------
# TomTom API for geocoding
# Get from: https://developer.tomtom.com
TOMTOM_API_KEY="your_tomtom_api_key"

# RapidAPI for flight search
# Get from: https://rapidapi.com
RAPIDAPI_KEY="your_rapidapi_key"

# ==================================
# IMPORTANT NOTES
# ==================================
# 1. Required fields MUST be filled in for the app to work
# 2. Generate strong passwords for SESSION_SECRET and MINIO_SECRET_KEY
# 3. Never commit deploy.config to version control (it's in .gitignore)
# 4. Keep this file secure - it contains sensitive credentials
# 5. For production, use real Stripe live keys (sk_live_* and pk_live_*)
