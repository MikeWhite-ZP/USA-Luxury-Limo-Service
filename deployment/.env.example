# ============================================
# USA Luxury Limo - Production Environment Variables
# ============================================
# Copy this file and configure for your production environment
# For Coolify: Add these in the UI under "Environment Variables"

# ============================================
# APPLICATION
# ============================================
NODE_ENV=production

# ============================================
# DATABASE (External PostgreSQL)
# ============================================
# CRITICAL: URL-encode special characters in password
# Example: If password is "Erka75810916?" encode as "Erka75810916%3F"
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD_HERE@postgres-container:5432/postgres

# ============================================
# SESSION
# ============================================
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-here-change-this

# ============================================
# MINIO / OBJECT STORAGE (External MinIO)
# ============================================
MINIO_ENDPOINT=https://minio.best-chauffeurs.com
MINIO_PORT=443
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=your-minio-access-key
MINIO_SECRET_KEY=your-minio-secret-key
MINIO_BUCKET=replit

# ============================================
# REPLIT AUTH (OIDC)
# ============================================
ISSUER_URL=https://replit.com
CLIENT_ID=your-replit-client-id
CLIENT_SECRET=your-replit-client-secret

# ============================================
# ADMIN PANEL ACCESS CONTROL
# ============================================
# Comma-separated list of allowed hostnames for admin access
ADMIN_PANEL_HOSTS=adminaccess.best-chauffeurs.com,localhost:5000

# ============================================
# API KEYS - LOCATION & FLIGHT
# ============================================
TOMTOM_API_KEY=your-tomtom-api-key
AERODATABOX_API_KEY=your-aerodatabox-api-key

# ============================================
# PAYMENT PROVIDERS
# ============================================

# Stripe
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
VITE_STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key

# PayPal
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret

# Square
SQUARE_ACCESS_TOKEN=your-square-access-token
SQUARE_LOCATION_ID=your-square-location-id

# ============================================
# TWILIO (SMS)
# ============================================
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# ============================================
# EMAIL (NODEMAILER)
# ============================================
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@example.com
EMAIL_PASSWORD=your-email-password
EMAIL_FROM=USA Luxury Limo <noreply@best-chauffeurs.com>

# ============================================
# ENCRYPTION (Database URL Management)
# ============================================
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# ============================================
# NOTES
# ============================================
# 1. DATABASE_URL: Special characters in password MUST be URL-encoded
#    ? → %3F
#    @ → %40
#    # → %23
#    & → %26
#
# 2. For Coolify deployment:
#    - Add these variables in Coolify UI
#    - OR create .env file in project root
#    - Coolify will inject them into container
#
# 3. External services (PostgreSQL, MinIO):
#    - Use Docker network names as hostnames
#    - Example: postgres-abc123 (container name)
#    - Coolify creates networks automatically
#
# 4. Security:
#    - NEVER commit .env file to git
#    - Use strong, random values for secrets
#    - Rotate keys regularly
