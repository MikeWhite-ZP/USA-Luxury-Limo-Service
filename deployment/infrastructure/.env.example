# ============================================
# Central Infrastructure Configuration
# ============================================
# These are ADMIN credentials - keep secure!
# Individual tenants get their own credentials
# ============================================

# ==========================================
# PostgreSQL Admin (for creating tenant DBs)
# ==========================================
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=CHANGE_ME_super_secure_admin_password
POSTGRES_PORT=5432

# External access URL (for tenant connections)
# Use your server's IP or domain
POSTGRES_HOST=your-server-ip-or-domain

# ==========================================
# MinIO Admin (for creating tenant buckets)
# ==========================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_ME_super_secure_minio_password
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# External access URL (for tenant connections)
# Use your server's IP or domain
MINIO_HOST=your-server-ip-or-domain

# ==========================================
# Security Notes
# ==========================================
# 1. Never share these admin credentials with tenants
# 2. Use strong passwords (openssl rand -base64 32)
# 3. Consider firewall rules to restrict access
# 4. For production, use SSL/TLS termination
