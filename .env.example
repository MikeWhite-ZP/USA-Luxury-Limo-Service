# ==================================
# USA Luxury Limo - Environment Variables
# ==================================
# Copy this file to .env and fill in your values
# For Coolify deployment, set these in the Coolify UI

# ----------------------------------
# Node Environment
# ----------------------------------
NODE_ENV=production
PORT=5000

# ----------------------------------
# Database (PostgreSQL)
# ----------------------------------
# Get this from your Neon dashboard or your PostgreSQL provider
DATABASE_URL=postgresql://user:password@host:5432/database

# ----------------------------------
# Session Secret
# ----------------------------------
# Generate a random secret: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this

# ----------------------------------
# Stripe Payment Processing
# ----------------------------------
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
# Optional: For frontend (must be prefixed with VITE_)
VITE_STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key

# ----------------------------------
# Email Configuration (Nodemailer)
# ----------------------------------
# SMTP server settings for sending emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email sender details
EMAIL_FROM_NAME=USA Luxury Limo
EMAIL_FROM_ADDRESS=noreply@usaluxurylimo.com

# ----------------------------------
# SMS Configuration (Twilio)
# ----------------------------------
# Get these from Twilio dashboard: https://console.twilio.com
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ----------------------------------
# TomTom Geocoding API
# ----------------------------------
# Get API key from: https://developer.tomtom.com
TOMTOM_API_KEY=your_tomtom_api_key

# ----------------------------------
# RapidAPI (Flight Search)
# ----------------------------------
# Get API key from: https://rapidapi.com
RAPIDAPI_KEY=your_rapidapi_key

# ----------------------------------
# Object Storage Configuration
# ----------------------------------
# Choose ONE of the following storage options:

# OPTION 1: MinIO (Recommended for VPS - Self-hosted S3-compatible)
# MinIO endpoint (e.g., http://minio:9000 for Docker, http://your-vps-ip:9000 for VPS)
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin-secret-change-this
MINIO_BUCKET=usa-luxury-limo

# OPTION 2: AWS S3 (If using AWS instead of MinIO)
# S3_BUCKET=your-s3-bucket-name
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1

# OPTION 3: Replit Object Storage (Only for Replit deployments)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-replit-bucket-id

# ----------------------------------
# Authentication Configuration
# ----------------------------------
# Replit Auth (Optional - only needed for Replit deployments)
# Leave these empty for non-Replit deployments
# REPLIT_DOMAINS=your-repl-domain.replit.app
# REPL_ID=your-repl-id
# ISSUER_URL=https://replit.com/oidc

# ----------------------------------
# Domain Configuration
# ----------------------------------
# Your production domain (for email links, payment redirects, etc.)
# REPLIT_DEV_DOMAIN=your-domain.com

# ----------------------------------
# PayPal & Square (Optional)
# ----------------------------------
# If using PayPal
# PAYPAL_CLIENT_ID=your_paypal_client_id
# PAYPAL_CLIENT_SECRET=your_paypal_client_secret

# If using Square
# SQUARE_ACCESS_TOKEN=your_square_access_token
# SQUARE_LOCATION_ID=your_square_location_id

# ==================================
# Notes for Coolify Deployment
# ==================================
# 1. Set all environment variables in Coolify UI under "Environment Variables"
# 2. Mark sensitive variables as "Secrets"
# 3. For MinIO, deploy it as a separate service in Coolify
# 4. Use internal Docker network names for MINIO_ENDPOINT (e.g., http://minio:9000)
# 5. Database should be accessible from the app container
# 6. Generate strong random values for SESSION_SECRET and MINIO_SECRET_KEY
